{
  "version": 2,
  "buildCommand": "mkdir -p backend/public && cd frontend && npm install && npm run build",
  "outputDirectory": "backend/public",
  "installCommand": "npm install",
  "framework": null
}

